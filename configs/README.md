<!-- Documents why the configs folder exists: environment-specific config assets for deployment tiers. -->
# Configs

Contains deployment profiles (dev/staging/prod) such as YAML or JSON config files.
