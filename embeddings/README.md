<!-- Documents why the embeddings package exists: standardized vectorization and index build reproducibility. -->
# Embeddings

Contains embedding model wrappers, batching jobs, and vector index build scripts.
